//@ts-check
/**
 * Initialize the project
 * @param {object} config
 * @returns {boolean}
 */
export function init(config) {
    return true;
}
//@ts-check
/**
 * Exit the project
 * @param {number} code
 * @returns {void}
 */
export function exit(code) {
    console.log(code + 1);
}
